<!-- Play with byebug -->
<div class="container content">
  <div class="row">
    <div class="col-md-4">

      <% collection = [1, 2, 3, 4] %>

      <% collection.each do |item| %>
        <% byebug %>
        <h3><%= item %></h3>
      <% end %>
    </div>
  </div>
</div>
